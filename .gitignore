node_modules/
target/
# …including when it is a symlink pointing the build output elsewhere,
# which `target/` does not match.
target
dist/
coverage/
test-results.json
*.node
.env
.env.local
.DS_Store
*.log
.turbo/
.augmentation-check-*
